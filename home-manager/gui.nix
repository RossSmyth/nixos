# Common config for (GUI) desktop computers
{
  xdg.enable = true;
  programs = {
    firefox.enable = true;

    # TUI Spotify
    spotify-player.enable = true;

    # TODO: Add mpv config
    mpv.enable = true;

  };
}
